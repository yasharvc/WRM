@model IEnumerable<EFRepository.GoupMenu>
@{
	ViewBag.Title = "Index";
}
<div class="btn-group-vertical">
		@foreach (var item in Model)
		{
			<a role="button" class="btn btn-primary" href="@item.Link">@Html.Raw(item.Title)</a>
		}
</div>